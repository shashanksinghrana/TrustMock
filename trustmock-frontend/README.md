# TrustMock Frontend (React + TypeScript + Vite + Tailwind)

## Prereqs
- Node 18+

## Run
```bash
npm install
npm run dev
```
UI: http://localhost:5173

> The dev server proxies `/api` to `http://localhost:8080` (configure in `vite.config.ts`).
